{
  "name": "snapcap-extension",
  "version": "1.1.0",
  "description": "SnapCap Chrome Extension Build System (100% free for the community)",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --mode production",
    "build:prod": "vite build --mode production",
    "package": "node package-release.js",
    "release": "node scripts/generate-brand-assets.js && npm run build && node package-release.js",
    "test": "node sw-behavior-test.mjs background/service_worker.js",
    "test:all": "node sw-behavior-test.mjs background/service_worker.js && npm --prefix backend test",
    "test:backend": "npm --prefix backend test",
    "preview": "vite preview",
    "lint": "eslint --ext .js background/ content/ editor/ popup/ offscreen/ scripts/",
    "format": "prettier --write \"**/*.{js,json,css,html,md}\"",
    "format:check": "prettier --check \"**/*.{js,json,css,html,md}\""
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "terser": "^5.24.0",
    "eslint": "^8.56.0",
    "prettier": "^3.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
